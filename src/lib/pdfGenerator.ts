// Intentionally left empty: PDF generation has been removed from the application.
// Stubbed exports (no-op) are retained to avoid accidental re-introduction via old imports.
export type InstitutionFormData = any;
export type PatientFormData = any;

export const generateInstitutionReport = (_formData: InstitutionFormData, _results: any[]) => {};
export const generatePatientReport = (_formData: any, _results: any[]) => {};
export const generateSingleDiseaseReport = (_patientData: PatientFormData | any, _diseaseResult: any) => {};